export enum PRODUCT_TYPE {
    SeasonalBanner = "seasonal-items"
}
const query = { queryParams: { 'majorCategory': 'popular', 'category': null, 'subcategory':  null} };

export const CATEGORY_QUERY_PARAMETER = {
    query
}